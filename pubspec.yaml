name: water_quality_monitor
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0  # Updated HTTP package for making REST API calls
  cupertino_icons: ^1.0.8
  firebase_core: ^3.11.0
  firebase_database: ^11.3.2
  syncfusion_flutter_gauges: ^28.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
